---
# simulation properties
sim_properties:
  light_position: [0.0, 10.0, 10.0]

# solver parameters
solver_params:
  raiSim:
    erp: 0.2              # when erp_on = true
  bullet:
    erp: 0.2              # when erp_on = true
    erp2: 0.1             # when erp_on = true
    erp_friction: 0.01    # when erp_on = true
  ode:
    erp: 0.2    # when erp_on = true

# simulation constants
constant:
  num_floor: 6            # remark! doesn't work for MUJOCO
  num_base: 8
  T: 600
  dt: 0.01
...